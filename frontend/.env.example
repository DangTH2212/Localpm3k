# Không đặt REACT_APP_API_URL — request /api đi qua proxy CRA → backend :9999 (cùng origin, cookie OK).
# Chạy: ngrok http 3000 rồi mở app bằng URL ngrok (không localhost:3000).

# REACT_APP_PROXY_TARGET=http://localhost:9999

DANGEROUSLY_DISABLE_HOST_CHECK=true
HOST=0.0.0.0
